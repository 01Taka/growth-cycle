import React from 'react';

interface TestButtonProps {}

const TestButton: React.FC<TestButtonProps> = ({}) => {
  return (
    <div>
      <h1>TestButtonContent</h1>
    </div>
  );
};

export default TestButton;
