export interface SelectStudyTimeButtonProps {
  label: string;
  timeMin: number;
  explanations: string[];
  theme: {
    text: string;
    textSub: string;
    bg: string;
    border: string;
  };
}
